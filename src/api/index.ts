import { API_AUTH } from "./auth";
import { PLACES } from "./places";

const API = {
  AUTH: API_AUTH,
  PLACES: PLACES,
};

export default API;

export const QUERY_KEYS = {
  CITIES: "cities",
  INTERESTS: "interests",
  PLACES: "places",
};
